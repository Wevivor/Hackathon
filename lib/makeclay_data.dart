import 'package:flutter/material.dart';

class Clay {
  String nickname;
  String image;

  Clay(this.nickname, this.image);
}

class MakeClayData extends StatelessWidget {
  const MakeClayData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
